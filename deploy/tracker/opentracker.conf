# UDP announce is public. HTTP announce is only reachable through Caddy,
# which adds X-Forwarded-For; WANT_IP_FROM_PROXY makes opentracker use it
# for requests from the proxy address below.
listen.udp 0.0.0.0:6969
listen.tcp 127.0.0.1:6969
access.proxy 127.0.0.1
